'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation'; import {Leaf,LogIn,UserPlus} from 'lucide-react'; import type {FormEvent} from 'react';
export default function Auth(){const router=useRouter();const [mode,setMode]=useState<'login'|'signup'>('login');const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode,name,email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Authentication failed');router.push('/dashboard');router.refresh()}catch(e:any){setError(e.message)}finally{setBusy(false)}};return <div className="page"><div className="container auth-wrap"><div className="card auth-card"><div className="brand" style={{marginBottom:20}}><span className="brand-mark"><Leaf size={22}/></span>FarmNivo</div><div className="eyebrow">Farmer account</div><h1 className="h2">{mode==='login'?'Welcome back':'Create your farmer workspace'}</h1><p className="muted">Local authentication is included for this build. Add a production identity provider before public launch.</p><form onSubmit={submit} className="list">{mode==='signup'&&<input className="input" placeholder="Your name" value={name} onChange={e=>setName(e.target.value)} required/>}<input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Password (6+ characters)" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn btn-primary" disabled={busy}>{mode==='login'?<LogIn size={17}/>:<UserPlus size={17}/>} {busy?'Please wait…':mode==='login'?'Login':'Create account'}</button>{error&&<div className="alert">{error}</div>}</form><button className="btn btn-soft" onClick={()=>setMode(mode==='login'?'signup':'login')}>{mode==='login'?'Need an account? Sign up':'Already have an account? Login'}</button><p className="muted" style={{fontSize:12}}>Demo account: farmer@farmnivo.local / demo1234</p></div></div></div>}
