'use client'; import {useEffect,useState} from 'react'; import {CloudSun,MapPin} from 'lucide-react';
export default function Weather(){const [d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/weather?location=Telangana').then(r=>r.json()).then(setD)},[]);return <div className="page"><div className="container"><div className="eyebrow">Weather center</div><h1 className="h2">Farm weather workspace</h1><div className="card" style={{marginTop:20}}><div className="pill"><MapPin size={14}/>{d?.location||'Loading...'}</div><h3><CloudSun size={22}/> {d?.live?'Live forecast':'Provider not connected'}</h3><p className="muted">{d?.message}</p><div className="grid grid-3">{d?.forecast?.map((x:any)=><div className="stat" key={x.day}><b>{x.day}</b><span>{x.advice}</span></div>)}</div></div></div></div>}
