'use client'; import {useEffect,useState} from 'react'; import {ExternalLink,Filter} from 'lucide-react';
export default function Schemes(){const [items,setItems]=useState<any[]>([]);const [q,setQ]=useState('');useEffect(()=>{fetch('/api/schemes').then(r=>r.json()).then(d=>setItems(d.schemes||[]))},[]);const filtered=items.filter(x=>JSON.stringify(x).toLowerCase().includes(q.toLowerCase()));return <div className="page"><div className="container"><div className="eyebrow">Government Scheme Center</div><h1 className="h2">Agriculture schemes</h1><p className="muted">Scheme information should always be verified against the official source before a farmer applies.</p><div className="search-large" style={{marginTop:18}}><Filter size={18}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search scheme, state or category"/></div><div className="grid grid-3" style={{marginTop:20}}>{filtered.map(x=><div className="card feature" key={x.id}><div className="pill">{x.state} · {x.category}</div><h3>{x.name}</h3><p className="muted">{x.summary}</p><a className="btn btn-soft" href={x.source} target="_blank" rel="noreferrer">Official source <ExternalLink size={15}/></a></div>)}</div></div></div>}
