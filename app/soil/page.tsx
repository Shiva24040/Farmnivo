'use client'; import {useEffect,useState} from 'react';
export default function Soil(){const [r,setR]=useState<any[]>([]);const [f,setF]=useState({ph:'',nitrogen:'',phosphorus:'',potassium:'',notes:''});const load=()=>fetch('/api/soil').then(x=>x.json()).then(x=>setR(x.records||[]));useEffect(()=>{load()},[]);const save=async()=>{await fetch('/api/soil',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setF({ph:'',nitrogen:'',phosphorus:'',potassium:'',notes:''});load()};return <div className="page"><div className="container"><div className="eyebrow">Soil & Fertility</div><h1 className="h2">Record soil tests</h1><div className="card" style={{marginTop:20}}><div className="grid grid-4">{['ph','nitrogen','phosphorus','potassium'].map(k=><input className="input" key={k} placeholder={k} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}</div><textarea className="input" style={{marginTop:12}} placeholder="Notes" value={f.notes} onChange={e=>setF({...f,notes:e.target.value})}/><button className="btn btn-primary" style={{marginTop:12}} onClick={save}>Save soil test</button></div><div className="grid grid-3" style={{marginTop:20}}>{r.map(x=><div className="card" key={x.id}><b>pH {x.ph??'—'}</b><p className="muted">N {x.nitrogen??'—'} · P {x.phosphorus??'—'} · K {x.potassium??'—'}</p></div>)}</div></div></div>}
