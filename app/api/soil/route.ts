import {NextResponse} from 'next/server'; import {z} from 'zod'; import {prisma} from '../../../lib/prisma'; import {currentUser} from '../../../lib/auth';
const schema=z.object({farmId:z.string().optional(),ph:z.coerce.number().min(0).max(14).optional(),nitrogen:z.coerce.number().nonnegative().optional(),phosphorus:z.coerce.number().nonnegative().optional(),potassium:z.coerce.number().nonnegative().optional(),notes:z.string().max(500).optional()});
export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:'Login required'},{status:401});return NextResponse.json({records:await prisma.soilRecord.findMany({where:{userId:u.id},orderBy:{createdAt:'desc'}})});}
export async function POST(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'Login required'},{status:401});try{const v=schema.parse(await req.json());const r=await prisma.soilRecord.create({data:{...v,userId:u.id}});return NextResponse.json({record:r},{status:201});}catch{return NextResponse.json({error:'Invalid soil record'},{status:400});}}
