import {NextResponse} from 'next/server'; import {db} from '../../../lib/store'; export async function GET(){return NextResponse.json({farms:db.farms.length,crops:db.crops.length,livestock:db.animals.length,machinery:db.machinery.length,marketplace:db.marketplace.length,community:db.posts.length,reels:db.reels.length,services:db.services.length,schemes:db.schemes.length,unreadNotifications:db.notifications.filter(n=>!n.read).length})}
