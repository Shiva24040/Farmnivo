import {NextResponse} from 'next/server'; import {prisma} from '../../../lib/prisma'; import {currentUser} from '../../../lib/auth';
export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:'Login required'},{status:401});return NextResponse.json({experts:[{id:'agri-kvk',name:'Krishi Vigyan Kendra / Agriculture Expert',category:'Crop advisory',note:'Use your district KVK or Agriculture Department for verified local advice.'},{id:'vet',name:'Veterinary Expert',category:'Livestock',note:'For animal illness, contact a qualified veterinarian.'}]});}
export async function POST(req:Request){const u=await currentUser();if(!u)return NextResponse.json({error:'Login required'},{status:401});const b=await req.json();await prisma.booking.create({data:{userId:u.id,serviceType:'expert',referenceId:b.expertId||null,title:String(b.title||'Expert consultation'),notes:String(b.notes||'')}});return NextResponse.json({ok:true},{status:201});}
