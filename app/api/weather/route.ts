import {NextResponse} from 'next/server'; export async function GET(req:Request){const location=new URL(req.url).searchParams.get('location')||'Telangana';return NextResponse.json({location,live:false,message:'Live weather is not configured in this local build. Connect a verified weather provider before showing current conditions.',forecast:[{day:'Today',advice:'Check rainfall forecast before irrigation.'},{day:'Tomorrow',advice:'Inspect soil moisture and drainage.'},{day:'Next 2 days',advice:'Plan field work around local conditions.'}]})}
