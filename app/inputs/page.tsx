'use client'; import {useEffect,useState} from 'react';
export default function Inputs(){const [items,setItems]=useState<any[]>([]);const [f,setF]=useState({name:'',category:'Seed',quantity:'1',unit:'kg',cost:'0',status:'planned'});const load=()=>fetch('/api/inputs').then(r=>r.json()).then(d=>setItems(d.inputs||[]));useEffect(()=>{load()},[]);const add=async()=>{await fetch('/api/inputs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setF({...f,name:'',quantity:'1',cost:'0'});load()};return <div className="page"><div className="container"><div className="eyebrow">Seeds & Farm Inputs</div><h1 className="h2">Track farm inputs and costs</h1><div className="card" style={{marginTop:20}}><div className="grid grid-3"><input className="input" placeholder="Input name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><select className="input" value={f.category} onChange={e=>setF({...f,category:e.target.value})}><option>Seed</option><option>Fertilizer</option><option>Bio-input</option><option>Equipment</option></select><input className="input" placeholder="Cost" value={f.cost} onChange={e=>setF({...f,cost:e.target.value})}/></div><button className="btn btn-primary" style={{marginTop:12}} onClick={add}>Save input</button></div><div className="grid grid-3" style={{marginTop:20}}>{items.map(x=><div className="card" key={x.id}><h3>{x.name}</h3><p className="muted">{x.category} · {x.quantity} {x.unit} · ₹{x.cost}</p></div>)}</div></div></div>}
