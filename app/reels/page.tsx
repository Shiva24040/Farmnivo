'use client'; import {useEffect,useState} from 'react'; import {ExternalLink,Play,Plus,Send,Video} from 'lucide-react';
export default function Reels(){const [r,setR]=useState<any[]>([]);const [show,setShow]=useState(false);const [form,setForm]=useState({title:'',category:'Farming tips',description:'',videoUrl:''});const load=()=>fetch('/api/reels').then(x=>x.json()).then(x=>setR(x.reels||[]));useEffect(()=>{load()},[]);const save=async()=>{const res=await fetch('/api/reels',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(res.ok){setForm({title:'',category:'Farming tips',description:'',videoUrl:''});setShow(false);load()}else alert((await res.json()).error||'Invalid reel')};return <div className="page"><div className="container"><div style={{display:'flex',justifyContent:'space-between',alignItems:'end',gap:10}}><div><div className="eyebrow">AgriReels</div><h1 className="h2">Short videos for farming</h1><p className="muted">Publish a real video URL from your storage/CDN. No fake media is created.</p></div><button className="btn btn-primary" onClick={()=>setShow(!show)}><Plus size={16}/>Create reel</button></div>{show&&<div className="card" style={{marginTop:18}}><div className="grid grid-2"><input className="input" placeholder="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input className="input" placeholder="Category" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/></div><input className="input" style={{marginTop:12}} placeholder="Video URL" value={form.videoUrl} onChange={e=>setForm({...form,videoUrl:e.target.value})}/><textarea className="input" style={{marginTop:12}} placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button className="btn btn-primary" style={{marginTop:12}} onClick={save}><Send size={15}/>Publish</button></div>}<div className="grid grid-3" style={{marginTop:22}}>{r.map(x=><div className="card feature" key={x.id}><a href={x.videoUrl} target="_blank" rel="noreferrer" style={{height:250,borderRadius:18,display:'grid',placeItems:'center',background:'linear-gradient(145deg,#153e26,#5da65b)',color:'#fff'}}><Play size={44}/></a><h3>{x.title}</h3><span className="pill"><Video size={14}/>{x.category} · {x.views} views</span><p className="muted">{x.description}</p><a className="btn btn-soft" href={x.videoUrl} target="_blank" rel="noreferrer">Open video <ExternalLink size={15}/></a></div>)}</div></div></div>}
