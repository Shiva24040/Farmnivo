'use client'; import {useEffect,useState} from 'react'; import {Play,Video,Plus,Send} from 'lucide-react';
export default function Reels(){const [r,setR]=useState<any[]>([]);const [show,setShow]=useState(false);const [form,setForm]=useState({title:'',category:'Farming tips',description:''});const load=()=>fetch('/api/reels').then(x=>x.json()).then(x=>setR(x.reels||[]));useEffect(()=>{load()},[]);const save=async()=>{const res=await fetch('/api/reels',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(res.ok){setForm({title:'',category:'Farming tips',description:''});setShow(false);load()}};return <div className="page"><div className="container"><div style={{display:'flex',justifyContent:'space-between',alignItems:'end',gap:10}}><div><div className="eyebrow">AgriReels</div><h1 className="h2">Short videos for farming</h1><p className="muted">The content model is ready for object storage/video processing integration.</p></div><button className="btn btn-primary" onClick={()=>setShow(!show)}><Plus size={16}/>Create reel</button></div>{show&&<div className="card" style={{marginTop:18}}><div className="grid grid-2"><input className="input" placeholder="Title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input className="input" placeholder="Category" value={form.category} onChange={e=>setForm({...form,category:e.target.value})}/></div><textarea className="input" style={{marginTop:12}} placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button className="btn btn-primary" style={{marginTop:12}} onClick={save}><Send size={15}/>Publish card</button></div>}<div className="grid grid-3" style={{marginTop:22}}>{r.map(x=><div className="card feature" key={x.id}><div style={{height:250,borderRadius:18,background:'linear-gradient(145deg,#153e26,#5da65b)',display:'grid',placeItems:'center',color:'#fff'}}><Play size={44}/></div><h3>{x.title}</h3><span className="pill"><Video size={14}/>{x.category} · {x.views} views</span><p className="muted">{x.description}</p></div>)}</div></div></div>}
