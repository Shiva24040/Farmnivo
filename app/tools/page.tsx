'use client'; import {useState} from 'react'; import {Calculator,LandPlot,Percent,Scale} from 'lucide-react';
export default function Tools(){const [a,setA]=useState('4');const [from,setFrom]=useState('acres');const [price,setPrice]=useState('28');const [yieldPerAcre,setYield]=useState('2500');const area=Number(a)||0;const sq=from==='acres'?area*4046.8564224:area*10000;const revenue=(Number(price)||0)*(Number(yieldPerAcre)||0)*area;return <div className="page"><div className="container"><div className="eyebrow">Farm tools</div><h1 className="h2">Practical calculators</h1><div className="grid grid-3" style={{marginTop:22}}><div className="card"><Calculator size={24}/><h3>Area converter</h3><input className="input" value={a} onChange={e=>setA(e.target.value)} type="number"/><select className="input" value={from} onChange={e=>setFrom(e.target.value)}><option value="acres">Acres</option><option value="hectares">Hectares</option></select><p><b>{sq.toFixed(2)} m²</b></p></div><div className="card"><Scale size={24}/><h3>Yield planner</h3><input className="input" value={yieldPerAcre} onChange={e=>setYield(e.target.value)} type="number" placeholder="kg / acre"/><p className="muted">Estimated harvest: <b>{((Number(yieldPerAcre)||0)*area).toLocaleString()} kg</b></p></div><div className="card"><Percent size={24}/><h3>Revenue planner</h3><input className="input" value={price} onChange={e=>setPrice(e.target.value)} type="number" placeholder="price / kg"/><p className="muted">Illustrative gross revenue: <b>₹{revenue.toLocaleString()}</b></p><small className="muted">Not a market-price forecast.</small></div></div><div className="card" style={{marginTop:20}}><LandPlot size={24}/><h3>More tools</h3><p className="muted">Fertilizer planning, seed-rate, irrigation volume, break-even, EMI, machinery cost, post-harvest loss and crop-calendar tools can plug into the same farm context.</p></div></div></div>}
