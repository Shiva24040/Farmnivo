'use client'; import {useEffect,useState} from 'react'; import {Search as SearchIcon} from 'lucide-react';
export default function SearchPage(){const [q,setQ]=useState('');const [items,setItems]=useState<any[]>([]);const [loading,setLoading]=useState(false);const run=async(v=q)=>{if(!v.trim())return;setLoading(true);const r=await fetch('/api/search?q='+encodeURIComponent(v));const d=await r.json();setItems(d.results||[]);setLoading(false)};useEffect(()=>{const p=new URLSearchParams(location.search).get('q')||'';setQ(p);if(p)run(p)},[]);return <div className="page"><div className="container"><div className="eyebrow">Global search</div><h1 className="h2">Search FarmNivo</h1><div className="search-large"><SearchIcon size={19}/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')run()}} placeholder="Search crops, machinery, produce, posts..."/><button className="btn btn-primary" onClick={()=>run()}>{loading?'Searching…':'Search'}</button></div><div className="grid grid-3" style={{marginTop:20}}>{items.map(x=><div className="card" key={x.id+x.type}><div className="pill">{x.type}</div><h3>{x.title||x.name||x.author}</h3><p className="muted">{x.description||x.body||x.category||''}</p></div>)}{q&&!loading&&!items.length&&<div className="card"><p className="muted">No matching results.</p></div>}</div></div></div>}
