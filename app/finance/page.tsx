'use client'; import {useEffect,useState} from 'react';
export default function Finance(){const [d,setD]=useState<any>({records:[],summary:{income:0,expense:0,balance:0}});const [f,setF]=useState({type:'expense',category:'Seeds',amount:'',note:''});const load=()=>fetch('/api/finance').then(r=>r.json()).then(setD);useEffect(()=>{load()},[]);const add=async()=>{await fetch('/api/finance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setF({...f,amount:'',note:''});load()};return <div className="page"><div className="container"><div className="eyebrow">Finance</div><h1 className="h2">Farm income, expenses & profit</h1><div className="grid grid-3" style={{marginTop:20}}><div className="card"><b>Income</b><h2>₹{d.summary.income}</h2></div><div className="card"><b>Expenses</b><h2>₹{d.summary.expense}</h2></div><div className="card"><b>Balance</b><h2>₹{d.summary.balance}</h2></div></div><div className="card" style={{marginTop:20}}><div className="grid grid-3"><select className="input" value={f.type} onChange={e=>setF({...f,type:e.target.value})}><option value="expense">Expense</option><option value="income">Income</option></select><input className="input" placeholder="Category" value={f.category} onChange={e=>setF({...f,category:e.target.value})}/><input className="input" placeholder="Amount" value={f.amount} onChange={e=>setF({...f,amount:e.target.value})}/></div><button className="btn btn-primary" style={{marginTop:12}} onClick={add}>Add transaction</button></div></div></div>}
