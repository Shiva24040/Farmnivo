'use client'; import {useEffect,useState} from 'react'; import {Bell,Check} from 'lucide-react';
export default function Notifications(){const [items,setItems]=useState<any[]>([]);const load=()=>fetch('/api/notifications').then(r=>r.json()).then(d=>setItems(d.notifications||[]));useEffect(()=>{load()},[]);const read=async(id:string)=>{await fetch('/api/notifications',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});load()};return <div className="page"><div className="container"><div className="eyebrow">Alerts</div><h1 className="h2">Notifications</h1><div className="list" style={{marginTop:20}}>{items.map(n=><div className={`card notification ${n.read?'read':''}`} key={n.id}><div style={{display:'flex',justifyContent:'space-between',gap:15}}><div><h3><Bell size={17}/> {n.title}</h3><p className="muted">{n.body}</p></div>{!n.read&&<button className="btn btn-soft" onClick={()=>read(n.id)}><Check size={15}/>Mark read</button>}</div></div>)}{!items.length&&<div className="card"><p className="muted">No notifications yet.</p></div>}</div></div></div>}
