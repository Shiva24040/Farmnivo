'use client'; import {useEffect,useState} from 'react'; import {BriefcaseBusiness,MapPin} from 'lucide-react';
export default function Services(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch('/api/services').then(r=>r.json()).then(d=>setItems(d.services||[]))},[]);return <div className="page"><div className="container"><div className="eyebrow">Farm Services</div><h1 className="h2">Find people who can help on the farm</h1><p className="muted">The directory is ready for verified provider profiles, service requests, ratings and location-based discovery.</p><div className="grid grid-4" style={{marginTop:22}}>{items.map(x=><div className="card feature" key={x.id}><div className="iconbox"><BriefcaseBusiness size={20}/></div><h3>{x.title}</h3><p className="pill">{x.category}</p><p className="muted"><MapPin size={14}/> {x.location}</p><p className="muted">{x.description}</p><button className="btn btn-soft" onClick={()=>alert('Service request flow is ready for the provider/booking integration stage.')}>Request service</button></div>)}</div></div></div>}
