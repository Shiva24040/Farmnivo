'use client'; import {useEffect,useState} from 'react';
export default function Experts(){const [e,setE]=useState<any[]>([]);const load=()=>fetch('/api/experts').then(r=>r.json()).then(d=>setE(d.experts||[]));useEffect(()=>{load()},[]);const request=async(x:any)=>{await fetch('/api/experts',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({expertId:x.id,title:x.name})});alert('Consultation request created.');};return <div className="page"><div className="container"><div className="eyebrow">Ask an Expert</div><h1 className="h2">Agriculture & livestock support</h1><div className="grid grid-2" style={{marginTop:20}}>{e.map(x=><div className="card" key={x.id}><h3>{x.name}</h3><p className="pill">{x.category}</p><p className="muted">{x.note}</p><button className="btn btn-primary" onClick={()=>request(x)}>Request consultation</button></div>)}</div></div></div>}
