'use client'; import {useEffect,useState} from 'react';
export default function Insurance(){const [p,setP]=useState<any[]>([]);const [f,setF]=useState({provider:'',policyType:'PMFBY',crop:'',policyNo:'',premium:'',sumInsured:'',status:'active'});const load=()=>fetch('/api/insurance').then(r=>r.json()).then(d=>setP(d.policies||[]));useEffect(()=>{load()},[]);const add=async()=>{await fetch('/api/insurance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setF({...f,provider:'',policyNo:'',premium:'',sumInsured:''});load()};return <div className="page"><div className="container"><div className="eyebrow">Finance & Insurance</div><h1 className="h2">Track crop insurance policies</h1><div className="card" style={{marginTop:20}}><div className="grid grid-3"><input className="input" placeholder="Provider" value={f.provider} onChange={e=>setF({...f,provider:e.target.value})}/><input className="input" placeholder="Crop" value={f.crop} onChange={e=>setF({...f,crop:e.target.value})}/><input className="input" placeholder="Policy number" value={f.policyNo} onChange={e=>setF({...f,policyNo:e.target.value})}/></div><button className="btn btn-primary" style={{marginTop:12}} onClick={add}>Save policy</button></div><div className="grid grid-3" style={{marginTop:20}}>{p.map(x=><div className="card" key={x.id}><h3>{x.policyType}</h3><p className="muted">{x.provider} · {x.crop||'All crops'} · {x.status}</p></div>)}</div></div></div>}
