'use client'; import {useEffect,useState} from 'react'; import {LogOut,Save} from 'lucide-react'; import {useRouter} from 'next/navigation';
export default function Profile(){const router=useRouter();const [u,setU]=useState<any>(null);const [name,setName]=useState('');const [msg,setMsg]=useState('');useEffect(()=>{fetch('/api/profile').then(r=>r.json()).then(d=>{setU(d.user);setName(d.user?.name||'')})},[]);const save=async()=>{const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name})});const d=await r.json();if(r.ok){setU(d.user);setMsg('Profile saved.')}};const logout=async()=>{await fetch('/api/auth/logout',{method:'POST'});router.push('/auth')};if(!u)return <div className="page"><div className="container"><div className="card"><h1 className="h2">Profile</h1><p className="muted">You are not logged in.</p><a className="btn btn-primary" href="/auth">Login</a></div></div></div>;return <div className="page"><div className="container"><div className="eyebrow">Account</div><h1 className="h2">Farmer profile</h1><div className="card profile-card"><label className="eyebrow">Name</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/><label className="eyebrow">Email</label><input className="input" value={u.email} disabled/><label className="eyebrow">Role</label><div className="pill">{u.role}</div><div style={{display:'flex',gap:10,marginTop:15}}><button className="btn btn-primary" onClick={save}><Save size={17}/>Save</button><button className="btn btn-soft" onClick={logout}><LogOut size={17}/>Logout</button></div>{msg&&<p className="muted">{msg}</p>}</div></div></div>}
